// Phase 2 test target for the app (kill-on-sight) block engine. Notepad is a
// safe, low-stakes process to kill while validating. Replaced by the real app
// blocklist config in a later phase.
export const TEST_APPLIST: string[] = ["notepad.exe"];
